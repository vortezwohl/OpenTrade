interface:
  display_name: "OpenTrade CLI"
  short_description: "规划 opentrade 命令并解释输出与排障"
  default_prompt: "Use $opentrade to plan a real opentrade CLI command, explain the true defaults versus the skill's recommended defaults, and interpret the result in observation view unless raw is explicitly requested."