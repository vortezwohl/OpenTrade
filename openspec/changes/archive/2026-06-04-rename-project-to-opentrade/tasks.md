## 1. 迁移边界确认

- [x] 1.1 确认发布名、显示名、包目录名、CLI 主命令和短命令的最终映射关系。
- [x] 1.2 确认 `efinance` / `efi` 与 `efinance_cli` 必须被彻底移除，不保留兼容层或告警入口。
- [x] 1.3 盘点当前仓库内所有高优先级旧命名入口，包括打包配置、源码入口、测试、README 和主文档。

## 2. 项目与包名重命名实现

- [x] 2.1 调整 `pyproject.toml` 中的项目元数据、脚本入口和包发现配置，使主发布与主包路径指向 `opentrade`。
- [x] 2.2 将 `efinance_cli/` 主实现路径迁移到 `opentrade/`，并同步更新内部导入和入口模块。
- [x] 2.3 删除旧包与旧脚本入口，不保留任何兼容导入层或兼容命令映射。

## 3. CLI 与文档迁移

- [x] 3.1 将 CLI 主命令切换到 `opentrade`，短命令切换到 `optr`，并彻底移除 `efinance` / `efi`。
- [x] 3.2 更新 README、主文档和必要的活跃 OpenSpec 文档，使对外名称统一为 `opentrade` / `Open Trade`。
- [x] 3.3 仅在必要范围内更新测试结论、示例命令和迁移说明，避免无关历史文档扩散改动。

## 4. 验证与回归

- [x] 4.1 更新并运行受影响测试，覆盖新包导入与新 CLI 命令，并确认不存在旧命名兼容路径。
- [x] 4.2 运行 CLI 帮助与关键回归命令，确认 `opentrade` / `optr` 可用且行为一致。
- [x] 4.3 使用全文搜索复查仓库中的残留旧命名，清除当前活跃路径中的主路径引用。
